version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8

    working_directory: ~/repo

    steps:

    - checkout
    - run: npm install
    - run: npm run lint

    - run: apt-get update && apt-get install azure-cli

    - run: az --version



