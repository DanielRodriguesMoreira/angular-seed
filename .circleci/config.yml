version: 2
jobs:
  build:
    docker:
    - image: circleci/node:8

      working_directory: ~/repo

    steps:

    - add-ssh-keys:
        fingerprints:
        - "SO:ME:FIN:G:ER:PR:IN:T"

    - checkout
    - run: npm install
    - run: npm run lint

    #Deploy production
    - deploy:
        - scp -r angular-seed/* drmoreira@Test:/~/
