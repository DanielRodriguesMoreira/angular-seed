version: 2
jobs:
  build:
    docker:
      - image: microsoft/azure-cli

    steps:
      - checkout
      - run: az --version
      # replace with Azure's username and password
      - run: az login -u daniel.moreira@present-technologies.com -p Present6?
      #- run: ~/repo/scripts/test.sh



